{
  "name": "conference-kit-monorepo",
  "version": "0.0.4",
  "private": true,
  "packageManager": "bun@1.3.5",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/react build && bun run --cwd packages/nextjs build && bun run --cwd packages/vue build && bun run --cwd packages/ui-react build && bun run --cwd packages/ui-next build && bun run --cwd packages/ui-vue build && bun run --cwd packages/signaling-server build",
    "dev:signal": "bun run --cwd packages/signaling-server dev"
  }
}
